// import {} from '../main';

export const qs = (cssSelector) => {
    return document.querySelector(cssSelector)
}


export const qsa = (cssSelector) => {
    return document.querySelectorAll(cssSelector)
}

const PI = 3.141596;
export default PI;